/**
 * Rota /portal-contador — conteúdo renderizado pelo layout (PainelContadorView).
 * Etapas são abas SPA; a URL não muda ao abrir/fechar abas.
 */
export default function PainelContadorPage() {
  return null
}
