# @tremor/react declara peer react@^18; o projeto usa React 19 — instalação sem falha de peer
legacy-peer-deps=true

# Antivírus/proxy corporativo que re-assina HTTPS (evita UNABLE_TO_VERIFY_LEAF_SIGNATURE e npm travado)
strict-ssl=false
