'use client'

import {
  CatalogGroupHeader,
  type CatalogGroupHeaderProps,
} from '@/src/presentation/components/features/catalogo/CatalogGroupHeader'

export type ProdutosGroupHeaderProps = CatalogGroupHeaderProps

/** Cabeçalho de grupo da lista de produtos base — mesmo shell do cardápio por menu. */
export const ProdutosGroupHeader = CatalogGroupHeader
